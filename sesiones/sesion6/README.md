# Hito 2 (sesion por definir)

Estado actual del trabajo en esta carpeta.

## Funcionalidad prevista

1. Menu por niveles: principal, personaje y tienda.
2. Multiples personajes.
3. Compra en tienda.
4. Inventario con arrays de tamano fijo.
5. Equipo por slots mediante `IEquipable`.

## Ficheros de entrada

1. `personajes.txt` con bloques `PERSONAJE / EQUIPO / INV / FIN`.
2. `tienda.txt` con lineas `ARMA / ARMADURA / METAL`.

## Restricciones de estilo

1. Sin `ArrayList`.
2. Sin `continue`.
3. Sin `break` fuera de `switch`.
4. Un solo `return` por funcion.
